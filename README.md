# DAY6

SQL Query on Sales_data

Used EXTRACT(MONTH FROM order_date) for month.

 GROUP BY year/month.
 
 Used SUM() for revenue.
 
 COUNT(DISTINCT order_id) for volume.
 
 Used ORDER BY for sorting.
